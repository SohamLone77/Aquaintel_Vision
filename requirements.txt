# ── Core ML stack ──────────────────────────────────────────────────────────
# Upper bounds guard against accidental major-version upgrades that
# introduce breaking API changes (TF 3.x, Ultralytics 9.x, etc.)
tensorflow>=2.13,<2.19      # TF 3.x is a separate project — keep on 2.x
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0

# ── Computer vision ─────────────────────────────────────────────────────────
opencv-python>=4.8,<5.0
pillow>=10.0,<12.0          # PIL fork; major versions can break EXIF/color API
ultralytics>=8.2,<9.0       # YOLO training/inference wrapper

# ── Data science ────────────────────────────────────────────────────────────
numpy>=1.24,<2.1            # NumPy 2.x changed many array APIs
matplotlib>=3.7,<4.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
seaborn>=0.13,<1.0

# ── Serving & UI ─────────────────────────────────────────────────────────────
streamlit>=1.38,<2.0        # Streamlit 2.x is not yet released; cap for safety
plotly>=5.17.0,<6.0

# ── Utilities ────────────────────────────────────────────────────────────────
PyYAML>=6.0,<7.0
requests>=2.31,<3.0
tqdm>=4.66,<5.0
imageio-ffmpeg>=0.6,<1.0

# ── Augmentation / ONNX ──────────────────────────────────────────────────────
albumentations>=1.4,<2.0
onnx>=1.12,<2.0
onnxruntime>=1.17,<2.0
onnxslim>=0.1.71,<1.0
